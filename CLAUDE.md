# CLAUDE.md — SOAM MEDIA 専用

このリポジトリで扱う事業は **SOAM MEDIA（アフィリエイト横断メディア）だけ**。

## 1. 現在仕様の唯一の正本

**`PRODUCT_SPEC.md`（このリポジトリのroot）が、現在の商品仕様の唯一の正本。**

- 現在仕様を判断するときは、`PRODUCT_SPEC.md` **以外の過去仕様書を正本として読まない。**
- Founderの新しい決定は、**同じ `PRODUCT_SPEC.md` へ上書きする。** 新しい仕様書ファイルを増やさない。
- 上書きで消える過去値は、`PRODUCT_SPEC.md` 末尾の `## Decision History` にだけ残す。
  **Decision History に書かれた値を現在値として読まない。**
- `docs/CURRENT.md` ／ `docs/CHANGELOG.md` は**実装の詳細な経緯の記録**であり、現在仕様の正本ではない。
  実装の経緯を追うときだけ読む。仕様の食い違いを見つけたら、統合せず `PRODUCT_SPEC.md` の
  「🟥 未確認・要Founder確認」へ両方の場所を書いて止める。
- **現在のコードを仕様より優先しない。** コードが `PRODUCT_SPEC.md` と違っていたら、
  それは「仕様が未決定」ではなく「**実装が正本へ未追従**」として記録する。勝手にどちらかへ寄せない。

## 2. 他事業に触らない

MimiとRami ／ SOAM Link ／ SOAM Start ／ SOAM Travel のファイルを、**検索・参照・変更しない。**
それらの事業の話が出てきても、このリポジトリでは扱わない。必要ならFounderへ「別の部屋の作業」と伝えて止まる。

SOAM Link への外向け導線（`https://link.soam-creative.com/`）は本リポジトリの設定として扱ってよいが、
SOAM Link 側の実装・仕様には立ち入らない。

## 3. この事業だけの例外（🟢 無人での外部公開）

**この事業は、無人での外部公開がFounder許可済みの唯一の事業**（Founder明示許可・2026-07-08）。
全事業共通の「AIが独断で外部行動を始めない／無人での外部投稿は禁止」に対する例外はここだけ。

🟥 **例外の対象は、このリポジトリの定時記事公開（`generate-and-publish-media-article.yml`）のみ。**
他の機能・他の事業・他の外部送信へ広げない。新しい無人処理を勝手に増やさない。

あわせて固定条件：

- **有料の記事生成は1公開枠につき1回。** 品質チェックに落ちた時だけ1回だけ書き直し（上限2回・Founder承認2026-08-07）。無制限の再生成は禁止。
- **手動起動の成功を「使える」と呼ばない。** 定時公開は**無人トリガー（schedule）での完走記録**だけが証拠になる。
  「一度動いた」と「無人で使える」を同じ欄に書かない。

## 4. 保存の仕方（GitHubが永続保存先）

- **GitHub remote（`soamcreativeai/soam-creative-media`）がこの事業の永続保存先。**
  Macの中にcommitがあるだけの状態を「保存済み」と呼ばない。
- 作業が終わったら **commit → push** まで行う。作業branchへの通常のpushは保存操作であり、公開ではない。
- **push と production公開・deploy は別の操作。** リポジトリへ保存しただけでdeployしない。

## 5. Founder Gate（勝手に進めない）

次はFounderの明示承認が要る。思いついても、見つけても、実行しない。提案として書くだけ。

- production deploy ／ 本番公開 ／ 配信対象ページの追加・除外の変更
- 金銭に関わること（有料生成の上限変更・課金・返金・送金・アフィリエイト契約の申請）
- Secret の作成・変更・表示（`MEDIA_AI_*` ／ `CLOUDFLARE_*` の値は読まない・書かない・貼らない）
- DNS・ドメインの変更
- 記事・公開URLの削除、検索除外、転送
- `shindan-ai-pro.html` を本番配信対象へ戻すこと（有料予定・決済導線が無いため 2026-08-07 に除外済み）

## 6. 誠実性（この事業は嘘がそのまま実害になる）

- 広告リンクのURL・ASP名・報酬条件を推測で作らない。`automation/affiliate-catalog.json` の
  `active: true` かつ `lastVerifiedAt` を持つ案件だけを使う。
- 紹介するツールの料金・機能・キャンペーンを捏造しない。確認できないなら書かない。
- 捏造レビュー・実在しない体験談・断定的な成功保証を書かない。
- アフィリエイト広告であることを隠さない。
- 「広告リンクが無い＝正常」と決めつけない。案件が無いのか、`isOfferRelevant` に弾かれたのかを必ず区別する。

## 7. 参照しないもの

- **Codex Challenger は参照しない。**
- 廃止済みの旧Cloudflare Worker（`aff-media.js` ／ `worker.js`）は**実在しない**。実装正本として参照しない。
- 過去の監査資料・候補ファイルを、現在仕様の根拠に使わない（`docs/` 配下の日付つき監査資料は証拠であって仕様ではない）。

## 8. 作業開始時にやること

1. `PRODUCT_SPEC.md` を実際に開く（現在仕様はここだけ）。
2. 断定する前に実物を見る：`.github/workflows/` ／ `scripts/` ／ `articles/data/manifest.json` ／ `automation/affiliate-catalog.json`。
3. 「完了した」と書く前に、実行したコマンドとその出力を証拠として出す。出せないものは「未確認」と書く。
